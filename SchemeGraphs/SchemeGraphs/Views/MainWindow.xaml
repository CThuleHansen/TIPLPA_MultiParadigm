<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:oxy="http://oxyplot.codeplex.com"
    x:Class="SchemeGraphs.MainWindow"
    Title="Simple Scheme Embedding" Height="500" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="193"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Margin="10,0,1,0">
            <Label FontWeight="Bold" Content="Function:"/>
            <TextBox x:Name="tb_function" Text="(lambda (x) (* x x))" Height="36" />
            <Label FontWeight="Bold" Content="X From:"/>
            <TextBox x:Name="tb_xfrom" Text="1" Height="18"/>
            <Label FontWeight="Bold" Content="X To:"/>
            <TextBox x:Name="tb_xto" Text="5" Height="18"/>
            <Label FontWeight="Bold" Content="Dx:"></Label>
            <TextBox x:Name="tb_dx"></TextBox>
            <Label FontWeight="Bold" Content="Samples:"/>
            <TextBox x:Name="tb_samples" Text="5" Height="18"/>

            <CheckBox Content="Derrivative"></CheckBox>
            <CheckBox Content="Integral" IsEnabled="False"></CheckBox>

            <Label Content="Scale:"></Label>
            <RadioButton Name="rb_linear" GroupName="rb_scale" Content="Linear" IsChecked="True"></RadioButton>
            <RadioButton Name="rb_log" GroupName="rb_scale" Content="Log10"></RadioButton>

            <Button Click="Evaluate_Click" Content="Draw"/>
            <Button Content="Clear" Click="Clear_Click"></Button>

            <Label FontWeight="Bold" Content="Output:"/>
            <TextBox x:Name="tb_output" Height="76" IsReadOnly="True"/>

        </StackPanel>
        <oxy:PlotView Model="{Binding MyModel}" Grid.Column="1" Margin="67,0,0,0"/>
    </Grid>
</Window>